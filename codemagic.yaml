workflows:

  web:
    name: Web
    max_build_duration: 30
    environment:
      flutter: default
      groups:
        - Firebase
    scripts:
      - flutter upgrade
      - flutter build web --release
 